# TODOs

for each path, put the variables in the gloabl scope and create an array per shape (for utility purposes).

compute the bounding box of the file using the verteces and place them in the script as global variables. automatically transform to the center of this bounding box in the applyTransform function but output is as commented out
